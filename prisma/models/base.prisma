enum Role {
    ADMIN
    BUSINESS
    CUSTOMER
}

enum TokenType {
    ACCESS
    REFRESH
}

enum OTPType {
    RESET_PASSWORD_OTP   //Reset Password 
    RESET_EMAIL_OTP   //Change Email 
    VERIFY_OTP   //Verify When Register 
}

enum OrderStatus {
    PENDING
    CONFIRMED
    PREPARING
    DELIVERING
    DELIVERED
    CANCELLED
}

enum PaymentMethod {
    ZALOPAY
    MOMO
    BANK
    CASH
}

enum PaymentStatus {
    FAILED
    SOLVING
    DONE
}

