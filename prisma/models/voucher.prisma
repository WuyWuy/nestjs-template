model Voucher {
    id          Int           @id @default(autoincrement())
    name        String
    description String        @default("")
    sale        Float  
    type        VoucherType
    status      VoucherStatus
    deleteAt    DateTime?
    orders      Order[]
}
