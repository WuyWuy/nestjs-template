model Voucher {
    id          Int           @id @default(autoincrement())
    name        String
    description String        @default("")
    sale        Decimal
    type        VoucherType
    status      VoucherStatus
    deleteAt    DateTime?
    orders      Order[]
}
